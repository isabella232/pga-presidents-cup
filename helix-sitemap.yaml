version: 1

sitemaps:
  website:
    origin: https://www.presidentscup.com
    lastmod: YYYY-MM-DD
    languages:
      default:
        source: /query-index.json
        destination: /sitemap.xml
        hreflang: en
        alternate: /{path}
